server:
  port: 9000
swagger:
  host: localhost:9000

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/test2;NON_KEYWORDS=USER
    username: sa
    password:
  hikari:
    maximum-pool-size: 2
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

  jpa:
    database: h2   # 추가 해준 부분
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      show_sql: false
      format_sql: true
    show-sql: false
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
  security:
    oauth2:
      client:
        registration:
          kakao:
            authorization-grant-type: authorization_code
            client-id: 2ffdee4e9bf274b66c0aa6e0c27cbc9f
            client-secret : 9T57ij5ETFlT1OIPhomy8POorHFUYD20
            redirect-uri: http://localhost:9000/login/oauth2/code/kakao
            scope:
              - profile_nickname
              - account_email
            client-authentication-method: POST
            client-name: kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: kakao_account

jwt:
  time:
    access: 1800000 # 30분
    refresh: 7776000000 # 90일