server:
  port: 9000

spring:
  profiles:
    include: secret

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:

    username:
    password:

  jpa:
    database:
    database-platform:
    hibernate:
      ddl-auto:
    show-sql:
    properties:
      hibernate:
        format_sql:

  cache:
    type:
  redis:
    host:
    port:

jwt:
  time:
    access:
    refresh:

